---
name: create-project-docs
trigger: /create-project-docs
description: Generate business logic documentation for existing projects
framework_command: {framework_commands}/create-project-docs.md
---

# Create Project Docs

This skill registers the `create-project-docs` command from the Scrum framework.

**Framework Command File:** `{framework_commands}/create-project-docs.md`

**Note:** `{framework_commands}` resolves to `{framework_path}/commands/` where `framework_path` is set in `config.yaml`.

Load the framework command file to execute this command.
