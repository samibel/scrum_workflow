---
name: create-ticket
trigger: /create-ticket
description: Create structured story from natural language for Scrum framework
framework_command: {framework_commands}/create-ticket.md
---

# Create Ticket

This skill registers the `create-ticket` command from the Scrum framework.

**Framework Command File:** `{framework_commands}/create-ticket.md`

**Note:** `{framework_commands}` resolves to `{framework_path}/commands/` where `framework_path` is set in `config.yaml`.

Load the framework command file to execute this command.
