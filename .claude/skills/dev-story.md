---
name: dev-story
trigger: /dev-story
description: Implement story following specification for Scrum framework
framework_command: {framework_commands}/dev-story.md
---

# Dev Story

This skill registers the `dev-story` command from the Scrum framework.

**Framework Command File:** `{framework_commands}/dev-story.md`

**Note:** `{framework_commands}` resolves to `{framework_path}/commands/` where `framework_path` is set in `config.yaml`.

Load the framework command file to execute this command.
