---
name: refine-ticket
trigger: /refine-ticket
description: Multi-agent refinement with Architect/Dev/QA perspectives for Scrum framework
framework_command: {framework_commands}/refine-ticket.md
---

# Refine Ticket

This skill registers the `refine-ticket` command from the Scrum framework.

**Framework Command File:** `{framework_commands}/refine-ticket.md`

**Note:** `{framework_commands}` resolves to `{framework_path}/commands/` where `framework_path` is set in `config.yaml`.

Load the framework command file to execute this command.
