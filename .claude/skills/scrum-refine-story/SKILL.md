---
name: scrum-refine-story
trigger: /scrum-refine-story
description: Validate story completeness using the Feature List as Immutable Contract pattern. Use when the user says 'refine story', 'validate story', or '/scrum-refine-story'.
framework_command: "{framework_commands}/refine-story.md"
---

# Refine Story (Validation Agent)

This skill registers the `refine-story` command from the Scrum framework.

**Framework Command File:** `{framework_commands}/refine-story.md`

**Note:** `{framework_commands}` resolves to `{framework_path}/commands/` where `framework_path` is set in `config.yaml`.

Load the framework command file to execute this command.
