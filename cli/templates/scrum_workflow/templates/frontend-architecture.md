# Frontend Architecture Documentation

Generated by `/scrum-create-architecture-docs` — Frontend architecture analysis.

## Overview

This document describes the frontend architectural components of the system, including component hierarchy, state management, routing structure, build pipeline, and shared utilities. The analysis is performed by the architect-doc agent using Glob+Grep patterns to identify frontend components across multiple frameworks.

**Analysis Date**: {{scan_date}}
**Analysis Mode**: {{scan_mode}} (full-scan / update)

## Component Hierarchy

The frontend uses the following component structure:

| Component | Location (file:line) | Parent | Description |
|-----------|---------------------|--------|-------------|
| *Components will be discovered and documented here...* | | | |

*Component hierarchy organized by feature/domain.*

```mermaid
graph TD
    App[App Component] --> HomePage[HomePage]
    App --> AuthLayout[AuthLayout]
    AuthLayout --> Login[LoginPage]
    AuthLayout --> Register[RegisterPage]
    HomePage --> Dashboard[Dashboard]
    HomePage --> Settings[SettingsPage]

    'Component hierarchy visualization will be generated here...'
```

## State Management

The application uses the following state management:

| Store/State | Type | Location (file:line) | Purpose |
|-------------|------|----------------------|---------|
| *State management will be discovered and documented here...* | | | |

```mermaid
flowchart
    Store[Global Store] --> AuthState[Auth State]
    Store --> UserState[User State]
    Store --> UIState[UI State]

    Actions[User Actions] --> Store
    Store --> Components[Component Access]

    'State management flow visualization will be generated here...'
```

## Routing Structure

| Route | Component (file:line) | Guards/Middleware | Description |
|-------|----------------------|-------------------|-------------|
| *Routes will be discovered and documented here...* | | | |

*Public routes*, *Protected routes* (require authentication), *Admin routes* (require authorization)

## Build Pipeline

| Tool | Config File (file:line) | Description |
|------|------------------------|-------------|
| *Build tools will be discovered and documented here...* | | |

*Bundler, transpiler, linter configuration.*

## Shared Utilities

| Utility/Hook | Location (file:line) | Description |
|--------------|---------------------|-------------|
| *Utilities will be discovered and documented here...* | | |

*Reusable functions, custom hooks, helper modules.*

---

*This document is generated automatically by the architect-doc agent. Last updated: {{generated_date}}*
