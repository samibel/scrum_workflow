# Backend Architecture Documentation

Generated by `/scrum-create-architecture-docs` — Backend architecture analysis.

## Overview

This document describes the backend architectural components of the system, including API endpoints, event systems, scheduled tasks, middleware pipeline, service layer, and database access layer. The analysis is performed by the architect-doc agent using Glob+Grep patterns to identify backend components across multiple frameworks.

**Analysis Date**: {{scan_date}}
**Analysis Mode**: {{scan_mode}} (full-scan / update)

## API Endpoints

| HTTP Method | Path | Controller (file:line) | Description |
|--------------|------|----------------------|-------------|
| *API endpoints will be discovered and documented here...* | | | |

*Grouped by resource/domain: Authentication, Users, Billing, etc.*

## Event System

The system uses the following event-driven components:

| Event Name | Publisher (file:line) | Subscriber(s) (file:line) | Description |
|-------------|---------------------|-------------------------|-------------|
| *Events will be discovered and documented here...* | | | |

```mermaid
sequenceDiagram
    participant Publisher
    participant EventBus
    participant Subscriber

    Publisher->>EventBus: emit(event)
    EventBus->>Subscriber: on(event)
    Note over Subscriber: Event handling logic

    'Event flow visualization will be generated here...'
```

## Scheduled Tasks

| Task Name | Schedule (cron/interval) | Handler (file:line) | Description |
|-----------|------------------------|---------------------|-------------|
| *Scheduled tasks will be discovered and documented here...* | | | |

## Middleware Pipeline

Request processing flows through the following middleware chain:

```mermaid
flowchart LR
    Request[Incoming Request] --> Auth[Authentication]
    Auth --> Validation[Request Validation]
    Validation --> Logging[Request Logging]
    Logging --> RateLimit[Rate Limiting]
    RateLimit --> Controller[Controller Handler]
    Controller --> Response[Response]

    'Middleware pipeline visualization will be generated here...'
```

## Service Layer

Backend services and their dependencies:

```mermaid
graph TD
    API[API Layer] --> AuthService[Authentication Service]
    API --> UserService[User Service]
    API --> BusinessService[Business Logic Service]
    BusinessService --> DatabaseRepo[Database Repository]

    AuthService --> DatabaseRepo[(Database Access)]
    UserService --> DatabaseRepo

    'Service dependency visualization will be generated here...'
```

## Database Access Layer

| Entity/Table | ORM/Framework | Schema File (file:line) | Relationships |
|--------------|--------------|-----------------------|--------------|
| *Database entities will be discovered and documented here...* | | | |

---

*This document is generated automatically by the architect-doc agent. Last updated: {{generated_date}}*
